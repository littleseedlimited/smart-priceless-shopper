[phases.setup]
nixPkgs = ["nodejs", "python311", "gcc"]

[phases.install]
cmds = [
    "npm run install:all",
    "python3 -m venv .venv",
    ".venv/bin/pip install requests python-telegram-bot python-dotenv"
]

[phases.build]
cmds = ["npm run build:frontend"]

[start]
cmd = "npm start"
